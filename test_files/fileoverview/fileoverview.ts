/**
 * @fileoverview Tests that a file overview comment is retained.
 * @modName {some_modname}
 * @mods {some.target}
 */
import X from 'goog:closure.FileOverview'; // from //some/path

let x: X|null = null;
console.log(x);
