/**
 * @fileoverview This file vaguely resembles the shape of the clutz output of
 * an interface-with-values .js input.
 *
 * (Normally, tsickle will not emit an interface if it also contains values,
 * because in general such a pattern is inexpressible in Closure.  Because of
 * this, we must define this IFace in a d.ts file.)
 */

declare namespace export_import {
  interface IFace {
    a: string;
  }

  namespace IFace {
    class C {}
  }
}
